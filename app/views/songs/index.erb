<div class="list-group">

<% @songs.each do |song| %>

	<a href="#" class="list-group-item list-group-item-action">"<%= song.name %>" - <%= song.artist.name %> - <%= song.genres.collect {|genre| genre.name}.join(", ") %></a>

<% end %>

</div>